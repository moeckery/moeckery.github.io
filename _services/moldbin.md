---
title: moldbin
domain: moldbin.com
status: soon
tagline: Not open yet.
# `link` is the live URL. Leave it commented out while the service is `soon`
# so the site doesn't send anyone to a domain that isn't serving anything.
# link: https://moldbin.com
# repo: https://github.com/moeckery/moldbin
# launched: 2026
---

moldbin.com is claimed and reserved. It isn't built yet, so there's nothing to
sign up for and nothing to try — this page exists so the name has an address
and a status that stays honest while the work happens.

## What's here so far

A domain, a name, and an intention. That's it. When there's something running,
this page becomes the place that describes it.

<!--
  TO FILL IN when moldbin takes shape — replace the two sections above with the
  real thing. A service page reads best as:

    1. One paragraph: what it does, for whom, in plain words.
    2. "How it works" or "What you get": a short list, not a feature dump.
    3. Anything a visitor needs before they commit — pricing, limits, privacy.

  Then in the front matter above:
    - flip `status: soon` to `beta` or `live`
    - uncomment `link:` so the domain becomes clickable across the whole site
    - uncomment `repo:` and `launched:` if they apply

  Nothing else on the site needs editing — the homepage and /services/ pick
  up the new status and re-sort on their own.
-->
